{
  "version": "$version",
  "executions": {
    "make-assembly": {
      "phase": "package",
      "goals": ["single"],
      "configuration": {
        "finalName": "$finalName",
        "archive": {
          "manifest": {
            "mainClass": "$mainClass"
          }
        },
        "descriptorRefs": {
          "descriptorRef": "$descriptorRef"
        },
        "appendAssemblyId": "$appendAssemblyId"
      }
    }
  }
}